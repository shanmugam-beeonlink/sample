name: Deployment Pipeline

on:
  push:
    branches:
      - main  # Only run after PRs are merged into main

jobs:
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/app:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/app:latest

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: echo "No tests yet" # Replace with `npm test` when ready

  migrate:
    name: Run Database Migration
    runs-on: ubuntu-latest
    needs: test
    environment:
      name: production         # Requires manual approval before running
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run DB migration
        run: |
          echo "Running DB migration"
          # Replace with your migration command
          # npx prisma migrate deploy

  deploy:
    name: Deploy Application
    runs-on: ubuntu-latest
    needs: migrate
    environment:
      name: production        # Shares same environment as migrate
      url: http://localhost:3000
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Pull and run Docker container
        run: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/app:latest
          docker run -d -p 3000:3000 --name app ${{ secrets.DOCKER_USERNAME }}/app:latest

      - name: Verify Deployment
        run: docker ps
